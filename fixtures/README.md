# Fixtures

Fixtures must support conformance testing across Python, TypeScript, Java, Go, and Rust SDKs.

Implemented fixture groups:

- `traces/`
- `destinations/`
- `policies/`
- `manifests/`
- `safe-traces/`
- `conformance/`
- `privacy-maps/`
- `replays/`

Validate fixtures:

```bash
bash ci/check-phase1.sh
```
