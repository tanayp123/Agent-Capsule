# Manifest Fixtures

Manifest fixtures cover signed capsule metadata, dependency hashes, prompt template hashes, tool definitions, model configuration, network destinations, required secrets, and usage meters.
