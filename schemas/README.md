# Schemas

Shared schemas define cross-language compatibility for SDKs, CLI, policy checks, safe trace export, manifests, and Agent Capsule Console.

Implemented files:

- `trace.schema.json`
- `destination.schema.json`
- `policy.schema.json`
- `manifest.schema.json`
- `safe-trace.schema.json`

Validate schemas and fixtures:

```bash
bash ci/check-phase1.sh
```
